import { ArchitectureNode } from './ArchitectureNode';
import { GroupNode } from './GroupNode';

export const nodeTypes = {
  architecture: ArchitectureNode,
  group: GroupNode,
};

export { ArchitectureNode, GroupNode };
